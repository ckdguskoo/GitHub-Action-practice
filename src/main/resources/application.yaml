spring:
  application:
    name: vlog
  datasource:
    url: jdbc:mysql://mysql:3306/practice
    username: root
    password: root1234

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
