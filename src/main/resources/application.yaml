spring:
  application:
    name: vlog
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: root1234

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
