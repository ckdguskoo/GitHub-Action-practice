spring:
  application:
    name: vlog
  datasource:
    #퍼블릭 ip에따라 수정해야함
    url: jdbc:mysql://13.125.100.147:3306/testdb
    username: root
    password: root1234

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
